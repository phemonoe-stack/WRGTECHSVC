// William Gannon && Todd Sewright// CS 162 Dr. Shirey Tu/Th 2pm-4pm// <wgannnon@mindspring.com>import java.text.NumberFormat;public class MusicCollection{	private MusicMedium[] collection;	private int total_entries, current_size;	private double total_value;	private NumberFormat dollar_value = NumberFormat.getCurrencyInstance();		public MusicCollection(){			current_size = 50;		collection = new MusicMedium[current_size];		total_entries = 0;		total_value = 0.00;		}		public void newMusicEntry(String name, String group, String[] list_tracks, int total_tracks,String music_medium, double cost){			if (total_entries == current_size)			increaseSize();					collection[total_entries] = new MusicMedium(name, group, 			list_tracks, total_tracks, music_medium, cost);		total_value += cost;		total_entries++;		}		private void increaseSize(){			MusicMedium[] temp_collection = new MusicMedium[current_size + 50];				for(int index = 0; index< current_size; index++)			temp_collection[index] = collection[index];				collection = temp_collection;		}		public void print(){				System.out.println("");		System.out.println("Total Entries: " + total_entries);		System.out.println("*************");		System.out.println("Value: " + dollar_value.format(total_value));		for(int index = 0; index < total_entries; index++){			collection[index].print();		}				}		public void selectionSort(){		MusicMedium[] objects = collection;		MusicMedium temp;		int min;				for(int index = 0; index < total_entries - 1; index++){			min = index;			for (int scan = index+1; scan < total_entries; scan++)				if (objects[scan].compareTo(objects[min]) < 0)					min = scan;								temp = objects[min];			objects[min] = objects[index];			objects[index] = temp;		}		collection = objects;	}			public void insertionSort(){		MusicMedium[] objects = collection;				for(int index = 1; index < total_entries; index++){					MusicMedium key = objects[index];			int position = index;						while(position > 0 && 				objects[position-1].compareTo(key)> 0){						objects[position] = objects[position-1];			position--;						}			objects[position] = key;		}			collection = objects;	}				}
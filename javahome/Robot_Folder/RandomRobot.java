import java.applet.Applet;import java.util.*;public class RandomRobot extends Robot{	private int direction_x, direction_y;	private Random move_random = new Random();	public RandomRobot(Applet applet, Obstacle an_obstacle, Goal your_goal, int x, int y){			super(applet, an_obstacle, your_goal, x, y);	}	public void move(){		int x_ray, y_ray;			direction_x = (Math.abs(move_random.nextInt()) %3) - 1;		direction_y = (Math.abs(move_random.nextInt()) %3) - 1;		x_ray = MOVE_BOT * direction_x + x_location;		y_ray = MOVE_BOT * direction_y + y_location;		// Check for obstacles			if(! an_obstacle.collision(x_ray, y_ray)){					x_location = x_ray;			y_location = y_ray;		}		else{			if(an_obstacle.collision(x_location, y_ray)){				direction_y = - direction_y;				x_location = x_ray;			}			else{				direction_x = - direction_x;				y_location = y_ray;			}		}		// Check if out of bounds		if(x_location <= 0){			x_location = 1;			direction_x = - direction_x;		}		else if (x_location > Room.length - Robot.IMAGE){			x_location = Room.length - Robot.IMAGE - 1;			direction_x = -direction_x;		}		if(y_location <= 0){			y_location = 1;			direction_y = - direction_y;		}		else if(y_location > Room.width - Robot.IMAGE){			y_location = Room.width - Robot.IMAGE - 1;			direction_y = -direction_y;		}	}}
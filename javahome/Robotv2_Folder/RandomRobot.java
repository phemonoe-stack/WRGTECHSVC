// William Gannon && Todd Sewright// CS 162 Dr. Shirey Tu/Th 2pm-4pm// <wgannnon@mindspring.com>import java.applet.*;import java.awt.*;import java.awt.image.*;import java.util.*;public class RandomRobot extends Robot{	private int direction_x, direction_y;	private Random move_random = new Random();	public RandomRobot(Applet this_applet, Obstacle[] this_obstacle, 		int num_obstacles, Goal this_goal, int this_x, int this_y, 			Image your_icon){			super(this_applet, this_obstacle, num_obstacles, this_goal, 			this_x, this_y, your_icon);	}	public void move(){		direction_x = (Math.abs(move_random.nextInt()) % 3) - 1;		direction_y = (Math.abs(move_random.nextInt()) % 3) - 1;		int x_ray = (MOVE_BOT * direction_x) + x_location;		int y_ray = (MOVE_BOT * direction_y) + y_location;		// Check for obstacles			for(int number = 0; number < total_obstacles; number++){			if(! your_obstacles[number].collision(x_ray, y_ray)){						x_location = x_ray;				y_location = y_ray;			}			else{ 				if(your_obstacles[number].collision					(x_location, y_ray)){					direction_y = - direction_y;					x_location = x_ray;				}				else{					direction_x = - direction_x;					y_location = y_ray;				}			}		}		// Check if out of bounds		if(x_location <= 0){			x_location = 1;			direction_x = - direction_x;		}		else if (x_location > Room.length - Robot.IMAGE_SIZE){			x_location = Room.length - Robot.IMAGE_SIZE - 1;			direction_x = - direction_x;		}		if(y_location <= 0){			y_location = 1;			direction_y = - direction_y;		}		else if(y_location > Room.width - Robot.IMAGE_SIZE){			y_location = Room.width - Robot.IMAGE_SIZE - 1;			direction_y = - direction_y;		}	}}
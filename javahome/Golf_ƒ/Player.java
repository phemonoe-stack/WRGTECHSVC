// William Gannon && Todd Sewright// CS 162 Dr. Shirey Tu/Th 2pm-4pm// <wgannnon@mindspring.com>public class Player{		final int MAX_HOLES = 18;	private String score_card = new String("");	private int[] player_swings, hole_par, player_score=new int[MAX_HOLES];	private int total_score, game_par, game_score;		public Player(int[] par, int[] swings){			player_swings = swings;		hole_par = par;			for(int index = 0; index<MAX_HOLES; index++){			player_score[index] = 				player_swings[index] - hole_par[index];			total_score += player_swings[index];						game_par += hole_par[index];		}		game_score =  total_score - game_par;	}	public int getTotalSwings(){return total_score;}		public int getGameScore(){return game_score;}	public String toString(){			for(int index = 0; index<MAX_HOLES; index++){			score_card += "Hole " + (index+1) + ": ";			score_card += player_score[index] + " \n";		}			score_card += "Total: " + game_score + " \r\n";		return score_card;		}	public static int whoWins(Player[] players){				int result = 0;		if(players[1].game_score>players[0].game_score){			if(players[2].game_score>players[0].game_score)				if(players[3].game_score>players[0].game_score)					result = 0;		}		if(players[0].game_score>players[1].game_score){			if(players[2].game_score>players[1].game_score)				if(players[3].game_score>players[1].game_score)					result = 1;		}		if(players[3].game_score>players[2].game_score){			if(players[1].game_score>players[2].game_score)				if(players[0].game_score>players[2].game_score)					result = 2;		}		if(players[2].game_score>players[3].game_score){			if(players[1].game_score>players[3].game_score)				if(players[0].game_score>players[3].game_score)					result = 3;		}		else			result = -1;		return result;			}// method compareTo}
// William Gannon && Todd Sewright// CS 162 Dr. Shirey Tu/Th 2pm-4pm// <wgannnon@mindspring.com>import java.applet.*;import java.awt.*;import java.awt.image.*;public class Obstacle{		private final int IMAGE_SIZE = 59, MASK = IMAGE_SIZE - 10;	private int my_x, my_y;	private Image my_icon;	protected Applet my_applet;	public Obstacle(Applet this_applet, int this_x, int this_y, 		Image obstacle_image){		my_applet = this_applet;		my_x = this_x;		my_y = this_y;		my_icon = obstacle_image;	}	public void draw(Graphics page){		page.drawImage(my_icon, my_x, my_y, my_applet);	}		public boolean collision(int your_x, int your_y){			int mask_x = your_x + MASK;		int mask_y = your_y + MASK;		boolean collided = false;				if (mask_x >= my_x - Robot.MASK)		     if (mask_x <= my_x + MASK + Robot.MASK)			 if (mask_y >= my_y - Robot.IMAGE_SIZE)			     if (mask_y <= my_y + MASK + Robot.IMAGE_SIZE)				  collided = true;		return collided;		}}
// William Gannon// <wgannnon@mindspring.com>class BankAccount {	private double balance;	private final int EMPTY_ACCOUNT = 0;	public BankAccount() {		balance = EMPTY_ACCOUNT;	}//constructor BankAccount	public BankAccount (double initial_balance) {		balance = initial_balance; 	}//constructor BankAccount		public void deposit (double deposit_amount) {		if (deposit_amount >= EMPTY_ACCOUNT) {			balance = balance + deposit_amount;	 	}	 	else	 		System.out.println(	 		"To apply for a loan, please speak with one of our service representitives."			);	}	public void withdrawl (double withdrawl_amount) {		if (withdrawl_amount >=EMPTY_ACCOUNT) {			balance -= withdrawl_amount;			if (balance < EMPTY_ACCOUNT){				balance += withdrawl_amount;								System.out.println(				"You do not have enough in this account" 				+ "\nPlease try again with a smaller amount."				);				}		}		else	 		System.out.println(	 		"If you would like to make a deposit, please try again."			);	} 				public double getBalance() {		return balance;	}	}//class BankAccount
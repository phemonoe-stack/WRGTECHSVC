// William Gannon && Todd Sewright// CS 162 Dr. Shirey Tu/Th 2pm-4pm// <wgannnon@mindspring.com>import java.io.*;import java.awt.*;import java.awt.event.*;import java.awt.event.ActionEvent;import java.awt.event.KeyEvent;import java.awt.BorderLayout;import javax.swing.*;import javax.swing.text.*;import javax.swing.event.*;import javax.swing.undo.*;import javax.swing.JFrame;import javax.swing.JLabel;import javax.swing.JPanel;import javax.swing.SwingConstants;public class OpenDialog extends JDialog{	private JLabel output; 	private JLabel enter = new JLabel("Enter Account Number: ");	private JButton accept, cancel, new_account, list;	private JTextField acc_field = new JTextField("", 10);	private JPanel one, two;	private Bank bank;	private ErrorDialog out;	private InfoDialog account_out;	private Frame framer;		public OpenDialog(Frame parent, boolean modal, Bank this_bank){			super(parent, modal);		setResizable(false);		setTitle("Welcome to Northfield Bank");		framer = parent;		bank = this_bank;		out = new ErrorDialog(framer, true);		new_account = new JButton("New Account");		new_account.setMnemonic('A');		new_account.addActionListener(new ActionListener(){			public void actionPerformed(ActionEvent e) {				NewAccountWindow window = new NewAccountWindow					(framer, true, bank);				window.setLocationRelativeTo(framer);				window.show();			}		});		accept = new JButton("Get Info");		accept.setMnemonic('I');		accept.addActionListener(new ActionListener(){			public void actionPerformed(ActionEvent e) {				Account needle;				needle = doSearch(bank, acc_field.getText());								if (needle == null){									out.setText(					    "No Info Found for Account: " 					    + acc_field.getText());					out.show();				}				else{					account_out = new InfoDialog						(framer, true, bank, needle);					account_out.setText(needle);					account_out.show();				}			}		});				cancel = new JButton("Quit");		cancel.setMnemonic('Q');		cancel.addActionListener(new ActionListener(){			public void actionPerformed(ActionEvent event){							dispose();				System.exit(0);						}		});		list = new JButton("List Accounts");		list.setMnemonic('L');		list.addActionListener(new ActionListener(){			public void actionPerformed(ActionEvent event){							BankDialog the_bank = new BankDialog					(framer, true, bank);				the_bank.show();			}		});		one = new JPanel();		two = new JPanel();		one.setLayout(new FlowLayout());		one.add(enter);		one.add(acc_field);		two.setLayout(new FlowLayout());		two.add(new_account);		two.add(list);		two.add(accept);		two.add(cancel);		getContentPane().setLayout(new BorderLayout(10,10));		getContentPane().add(one, BorderLayout.NORTH);		getContentPane().add(two, BorderLayout.SOUTH);	}	public Account doSearch(Bank this_bank, String target){		Account target_account;				target_account = this_bank.getAccountInfo(target);				return target_account;	}}
// William Gannon && Todd Sewright// CS 162 Dr. Shirey Tu/Th 2pm-4pm// <wgannnon@mindspring.com>import java.text.NumberFormat;public class Pizza{	private String the_pizza = new String("");;	private Topping[] toppings = new Topping[1];	private String size;	private double price, initial_price;	private NumberFormat dollar_value = NumberFormat.getCurrencyInstance();		public Pizza(String this_size){			size = this_size;		toppings[0] = new Topping("Cheese", 0.00);		price = 0.00;		if (size.equals("small") || size.equals("Small")){			price = 7.99;		}		else if (size.equals("medium") || size.equals("Medium")){			price = 8.99;		}		else if (size.equals("large") || size.equals("Large")){			price = 9.99;		}		}	public Pizza(Topping[] these_toppings, String this_size){			size = this_size;		if(size == null)			size = "Small";		toppings = these_toppings;		toppings[0] = new Topping("Cheese", 0.00);		if (size.equals("small") || size.equals("Small")){			initial_price = 7.99;		}		else if (size.equals("medium") || size.equals("Medium")){			initial_price = 8.99;		}		else if (size.equals("large") || size.equals("Large")){			initial_price = 9.99;		}		price += initial_price;			addToppings();	}		public void addToppings(){			for(int index = 0; index<toppings.length; index++){				price += toppings[index].getPrice();			}	}	public void setTopping(Topping[] these_toppings){			toppings = these_toppings;		addToppings();				}		public void setSize(String new_size){		size = new_size;	}	public void setPrice(double new_price){		price = new_price;	}	public Topping[] getToppings(){			return toppings;	}	public String getSize(){		return size;	}	public double getPrice(){		return price;	}	public String toString(){		the_pizza = "\r\n";		the_pizza += size + " " + dollar_value.format(initial_price);				for(int index = 0; index<toppings.length; index++){				the_pizza += toppings[index].toString();		}		the_pizza += "\r\nTotal: " + dollar_value.format(price);		return the_pizza;	}	public void print(){		}}